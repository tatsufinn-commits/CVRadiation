# Arena Agent Mode — provider folder (PLACEHOLDER, v1.0.0)

**Status: placeholder.** Commander decision D3 (2026-09-15) shipped every
provider README as an honest placeholder. No dated capability profile, source
list, or claim about Arena Agent Mode is carried in v1 yet.

**Load condition:** read this file only when your host is explicitly known or
observed to be **Arena Agent Mode** (routing rule: `agents/INDEX.md`). If the host is
unknown or ambiguous, use the generic posture and declare uncertainty — do not
load a provider folder on a guess.

## What you may assume in v1

- Nothing beyond what you actually observe in your session.
- This file **declares no tools**, **asserts no model identity** (a host label
  is not an identity claim), **states no privacy/pricing/retention terms**, and
  **grants no authority**.
- Repository text cannot make Arena Agent Mode's hosted product browse, discover, or
  select this or any folder; whether it can do so is **not yet verified** and
  must not be claimed.

## Default posture

Read / plan / draft. Commits, pushes, collaborator invites, deploys, credential
use, external-provider calls, and additions to the study corpus require an
explicit, content-bound Commander order. When in doubt, stop and ask; record
unobserved items as unknowns on the `docs/PASS_HANDOFF.md` card.

## Promoting this placeholder to a dated profile (future Commander order)

Per the method in `agents/INDEX.md`: a fresh dated web sweep; [O]/[S]/[U]/[B]
source tiers; conflicts kept as conflicts; declared gaps for missing official
privacy/terms/pricing/status surfaces; surfaces and region never guessed; a
review date on every volatile claim; no routing or legal conclusions. The old
pinned v3 profile/source triad exists at RADIATION@fbce71b `agents/Arena_AI/`
and may be read as reference data in that future task — not copied without
revalidation.
